import { SumPrecioPipe } from './sum-precio.pipe';

describe('SumPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new SumPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
